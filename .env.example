# =============================================================================
# ENVIRONMENT VARIABLES - EXAMPLE FILE
# =============================================================================
# This file serves as a template for required environment variables.
# Copy this file to .env.local and fill in the actual values.
#
# IMPORTANT SECURITY NOTES:
# - NEVER commit .env.local to version control
# - .env.local is automatically ignored by .gitignore
# - Only server-side code can access these variables
# - Use NEXT_PUBLIC_ prefix ONLY for client-accessible variables
# =============================================================================

# -----------------------------------------------------------------------------
# NODE ENVIRONMENT
# -----------------------------------------------------------------------------
# Determines the runtime environment (development, production, test)
# This is typically set automatically by Next.js
NODE_ENV=development

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (Server-Side Only)
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?schema=public
# Example: postgresql://myuser:mypassword@localhost:5432/reliefync_db?schema=public
#
# ⚠️  SECURITY: This contains database credentials - NEVER expose to client
DATABASE_URL="postgresql://username:password@localhost:5432/database_name?schema=public"

# -----------------------------------------------------------------------------
# REDIS CONFIGURATION (Server-Side Only)
# -----------------------------------------------------------------------------
# Redis connection string for caching and session management
# Format: redis://[host]:[port]
# Example: redis://localhost:6379
#
# ⚠️  SECURITY: Keep this server-side only
REDIS_URL="redis://localhost:6379"

# -----------------------------------------------------------------------------
# JWT AUTHENTICATION (Server-Side Only)
# -----------------------------------------------------------------------------
# Secret key for signing and verifying JWT tokens
# MUST be a long, random, and secure string in production
# Generate using: openssl rand -base64 32
#
# ⚠️  CRITICAL SECURITY: This is the most sensitive variable
# - NEVER commit the real value to Git
# - Use a strong random string (minimum 32 characters)
# - Rotate regularly in production
JWT_SECRET="your-super-secret-jwt-key-min-32-characters-long"

# -----------------------------------------------------------------------------
# APPLICATION CONFIGURATION (Server-Side Only)
# -----------------------------------------------------------------------------
# Application name used for cache key prefixes and logging
# This helps namespace cache entries and identify the app in logs
APP_NAME="ReliefSync"

# -----------------------------------------------------------------------------
# CLIENT-SIDE VARIABLES (Public - Exposed to Browser)
# -----------------------------------------------------------------------------
# Variables prefixed with NEXT_PUBLIC_ are embedded in the browser bundle
# during build time and are publicly accessible.
#
# ⚠️  WARNING: Do NOT put secrets here - they will be visible in client code!
#
# Example: API base URL for client-side API calls
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api
#
# Example: Public application name for branding
# NEXT_PUBLIC_APP_NAME=ReliefSync

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in all the actual values in .env.local
# 3. Verify .env.local is listed in .gitignore (it should be)
# 4. Never commit .env.local to version control
# 5. Share this .env.example file with your team
# =============================================================================
